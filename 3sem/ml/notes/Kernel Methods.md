
### Macer's theorem
A function $k: \mathcal{X} \times \mathcal{X} \rightarrow \mathbb{R}^+$ is a **kernel function**, if the

Gram matrix it generates (i.e. the matrix generated by evaluating the function on all pairs of data points in an arbitrary data set with $m$ data points)

  $$mat(k(x_1,x_1), dots, k(x_1,x_m); dots.v, dots.down, dots.v;k(x_n,x_1),dots,k(x_n,x_m))$$
$a/(b c) dot a$
$\frac{a}{bc} \cdot a$
$$