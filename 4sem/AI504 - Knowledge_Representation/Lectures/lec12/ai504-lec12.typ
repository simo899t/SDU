#import "../../../../temp/temp.typ": *
#show: note.with(
  title: ("Lecture 12: First Order Logic: Formalization (translations FO " + $<-> $ + " natural language)"),
  course: "AI504 - Knowledge Representation",
  date: "May/2026"
)
// content starts here

/*
Come by Siddarth's office or via email
*/

= What Siddarth expects
Knowing what $model ent phi$ means when $model$ is a FO model and $phi$ is a FO sentence.

Also formalization.

= Free and bounded variables
$ P(y) = underbrace(forall x, "bounded") (x+x=underbrace(y, "free")) $